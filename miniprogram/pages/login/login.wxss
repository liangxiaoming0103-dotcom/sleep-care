/* pages/login/login.wxss */
/* 登录页面 — 靛蓝睡眠主题 */

/* ===== 容器 ===== */
.container {
  width: 100vw;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: linear-gradient(180deg, #eef2ff 0%, #f5f3ff 40%, #fafafa 100%);
  padding: 0 56rpx;
  box-sizing: border-box;
}

/* ===== 品牌区 ===== */
.hero {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 64rpx;
}

.hero-icon {
  font-size: 96rpx;
  margin-bottom: 20rpx;
}

.hero-title {
  font-size: 44rpx;
  font-weight: 800;
  color: #1e1b4b;
  letter-spacing: 2rpx;
}

.hero-sub {
  font-size: 26rpx;
  color: #a5b4fc;
  margin-top: 12rpx;
  letter-spacing: 4rpx;
}

/* ===== 卡片 ===== */
.card {
  width: 100%;
  background: #fff;
  border-radius: 32rpx;
  padding: 52rpx 40rpx 40rpx;
  box-shadow: 0 8rpx 40rpx rgba(79, 70, 229, 0.08);
}

/* ===== 输入域 ===== */
.field {
  display: flex;
  align-items: center;
  background: #f5f7fe;
  border-radius: 20rpx;
  padding: 0 24rpx;
  margin-bottom: 28rpx;
  border: 2rpx solid transparent;
  transition: border-color 0.2s;
}

.field:focus-within {
  border-color: #818cf8;
  background: #fff;
}

.field-icon {
  font-size: 36rpx;
  margin-right: 16rpx;
  flex-shrink: 0;
}

.field-input {
  flex: 1;
  height: 96rpx;
  font-size: 30rpx;
  color: #1e1b4b;
}

/* placeholder 颜色 */
.field-input::placeholder {
  color: #c7d2fe;
}

/* ===== 按钮 ===== */
.btn {
  width: 100%;
  height: 96rpx;
  background: linear-gradient(135deg, #6366f1, #4f46e5);
  color: #fff;
  font-size: 32rpx;
  font-weight: 700;
  border-radius: 48rpx;
  border: none;
  margin-top: 16rpx;
  letter-spacing: 8rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8rpx 24rpx rgba(99, 102, 241, 0.30);
}

.btn:active {
  background: linear-gradient(135deg, #4f46e5, #4338ca);
  transform: scale(0.98);
}

/* ===== 底部 ===== */
.footer {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-top: 36rpx;
  gap: 14rpx;
}

.footer-hint {
  font-size: 22rpx;
  color: #c7d2fe;
}

.footer-link {
  font-size: 28rpx;
  color: #6366f1;
  font-weight: 600;
}

.footer-link:active {
  color: #4f46e5;
}
